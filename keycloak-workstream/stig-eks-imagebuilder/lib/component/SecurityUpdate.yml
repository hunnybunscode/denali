name: SecurityUpdate
description: Update security
schemaVersion: 1.0

phases:
  - name: build
    steps:
      - name: UpdateSecurity
        action: ExecuteBash
        inputs:
          commands:
            - |
              sudo yum update --assumeyes --security
