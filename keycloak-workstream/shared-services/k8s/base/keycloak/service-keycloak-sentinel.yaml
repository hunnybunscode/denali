apiVersion: v1
kind: Service
metadata:
  name: service-keycloak-sentinel
  namespace: keycloak
spec:
  type: LoadBalancer
  selector:
    keycloak: keycloak-sentinel
  ports:
    - name: ui
      port: 8443
