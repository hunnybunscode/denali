apiVersion: v1
kind: Service
metadata:
  name: service-keycloak-instance
  namespace: keycloak
spec:
  type: ClusterIP
  selector:
    keycloak: keycloak-instance
  ports:
    - name: ui
      port: 8443
