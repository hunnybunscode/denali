apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-keycloak-app-internal
  namespace: keycloak
  annotations:
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/actions.ssl-redirect: '{"Type": "redirect", "RedirectConfig": { "Protocol": "HTTPS", "Port": "443", "StatusCode": "HTTP_301"}}'
    alb.ingress.kubernetes.io/group.name: keycloak-web-ui-internal
    alb.ingress.kubernetes.io/backend-protocol: HTTPS
    alb.ingress.kubernetes.io/healthcheck-protocol: HTTPS
    alb.ingress.kubernetes.io/ssl-policy: ELBSecurityPolicy-TLS13-1-2-FIPS-2023-04
    alb.ingress.kubernetes.io/success-codes: 200-299,302
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/target-group-attributes: stickiness.enabled=true,stickiness.lb_cookie.duration_seconds=300
    alb.ingress.kubernetes.io/load-balancer-attributes: idle_timeout.timeout_seconds=160

spec:
  ingressClassName: alb
  rules:
    - host: sso.denali.jktruong.people.aws.dev.internal
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: service-keycloak-instance
                port:
                  name: ui
